<Window x:Class="Equalizer.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Equalizer — Ustawienia"
        Height="320" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="#FF1E1E1E">

    <!-- Faza 0: schludny placeholder. Suwaki pasm i presety dojda w Fazie 4. -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Equalizer"
                   Foreground="White"
                   FontSize="24"
                   FontWeight="SemiBold" />

        <TextBlock Grid.Row="1"
                   Margin="0,4,0,0"
                   Text="Faza 0 — szkielet. Brak przetwarzania dźwięku."
                   Foreground="#FFB0B0B0"
                   FontSize="13" />

        <Border Grid.Row="2"
                Margin="0,20,0,0"
                Padding="16"
                CornerRadius="8"
                Background="#FF2A2A2A"
                VerticalAlignment="Top">
            <CheckBox x:Name="AutostartCheckBox"
                      Content="Uruchamiaj razem z systemem"
                      Foreground="White"
                      FontSize="14"
                      Checked="AutostartCheckBox_Changed"
                      Unchecked="AutostartCheckBox_Changed" />
        </Border>

        <TextBlock Grid.Row="3"
                   Text="Okno można zamknąć — aplikacja działa dalej w zasobniku."
                   Foreground="#FF808080"
                   FontSize="11"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
